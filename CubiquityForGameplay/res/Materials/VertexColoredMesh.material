material VertexColoredMesh
{
    technique
    {
        pass 0
        {
            // shaders
            vertexShader = res/Materials/Shaders/VertexColoredMesh.vert
            fragmentShader = res/Materials/Shaders/VertexColoredMesh.frag

             // uniforms
            u_worldViewProjectionMatrix = WORLD_VIEW_PROJECTION_MATRIX
            u_inverseTransposeModelToWorldMatrix = INVERSE_TRANSPOSE_WORLD_MATRIX
            
            u_ambientColor = SCENE_AMBIENT_COLOR
            
            // render state
            renderState
            {
                cullFace = true
                depthTest = true
            }
        }
    }
}